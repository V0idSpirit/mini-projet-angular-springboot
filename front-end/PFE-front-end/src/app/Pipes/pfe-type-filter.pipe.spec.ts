import { PfeTypeFilterPipe } from './pfe-type-filter.pipe';

describe('PfeTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PfeTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
