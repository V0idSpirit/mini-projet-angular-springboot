import { PfeTitreFilterPipe } from './pfe-titre-filter.pipe';

describe('PfeTitreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PfeTitreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
